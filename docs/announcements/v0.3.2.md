# SpecOps v0.3.2

## 🐛 Fix

- **PyPI README rendering** — Added `content-type = "text/markdown"` to the readme field in `pyproject.toml` so badges, tables, and the banner image render correctly on the PyPI project page.

## 📥 Install

```bash
pip install specops-ai==0.3.2
```

**Full Changelog**: https://github.com/kripikroli/specops-ai/compare/v0.3.1...v0.3.2
